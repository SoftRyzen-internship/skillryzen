import { Input } from '../../../ui-kit/Input/Input';

const LoginPage = () => {
  // return <div>Login</div>;
  return <Input name='search' placeholder='Search' button />;
};

export default LoginPage;
